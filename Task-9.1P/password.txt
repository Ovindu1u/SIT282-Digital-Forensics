privatePassword

--do not use a txt file with the password as the file you generate the hash from, since text files typically include hidden special charecters such as new lines, it can mess with the hash
--it is much safer to create a new empty file in CryptTool, type to password to it and then hashing
